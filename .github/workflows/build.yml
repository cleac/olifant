name: Check-Build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-18.04

    steps:
    - name: Meson Build
      uses: BSFishy/meson-build@v1.0.1
      with:
        action: build
